import 'package:flutter/material.dart';

class TextFormFieldSizedbox extends StatelessWidget {
  const TextFormFieldSizedbox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 18,
    );
  }
}
